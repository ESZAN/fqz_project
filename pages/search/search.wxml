<!--pages/main/main.wxml-->
<view class='headView'>

    <view class='headView'>症状搜索</view>
  </view>
<view class="search_total">
            <view class="search_bar">
                <view class="search_bar_box">
                    <icon class="search_inbox" type="search" size="18"></icon>
                    <input type="text" class="search_bar_input" placeholder="请输入搜索内容"/>
                           </view>
                            </view>

            <!-- 搜索按钮,调用搜索查询方法 -->
            <view class="search_button" bindtap='searchfun'>搜索</view>
            </view>
<view class="table">
 <view class="tr bg-w">
 <view class="tha">head1</view>
 <view class="tha">head2</view>
 <view class="tha ">head3</view>
 </view>
 <block wx:for="{{listData}}" wx:key="{[code]}">
 <view class="tr bg-g" wx:if="{{index % 2 == 0}}">
 <view class="tda">{{item.code}}</view>
 <view class="tda">{{item.text}}</view>
 <view class="tda">{{item.type}}</view>
 </view>
 <!--<view class="tr" wx:else>-->
 <view class="tda">{{item.code}}</view>
 <view class="tda">{{item.text}}</view>
 <view class="tda">{{item.type}}</view>
 </view>
 </block>
</view>